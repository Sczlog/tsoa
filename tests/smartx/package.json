{
  "name": "@smartx/tsoa-test-smartx",
  "version": "0.0.1-rc.3",
  "main": "index.js",
  "author": "Sczlog",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/chai-as-promised": "^7.1.4",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.9.1",
    "chai": "^4.3.4",
    "chai-as-promised": "^7.1.1",
    "mocha": "^9.1.3",
    "ts-node": "^10.3.0",
    "tslib": "^2.3.0",
    "typescript": "^4.4.3",
    "snap-shot-it": "^7.9.6"
  },
  "scripts": {
    "pretest": "yarn clean && yarn prepare && tsc",
    "clean": "rimraf ./fixture/generated/*",
    "prepare": "ts-node prepare.ts",
    "posttest": "yarn clean",
    "test": "cross-env NODE_ENV=tsoa_test mocha --require=ts-node/register --require=tsconfig-paths/register --exit",
    "test:u": "cross-env NODE_ENV=tsoa_test SNAPSHOT_UPDATE=1 mocha --require=ts-node/register --require=tsconfig-paths/register --exit"
  }
}
